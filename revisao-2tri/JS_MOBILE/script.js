function olá(nome) {
  alert('olá ' + nome);
}
olá('josé');

const nome = 'julius '
const sobrenome = 'Rock '
const idade = '43'

function dados_pessoais() {
  const dados + nome + sobrenome + idade;
  console.log(dados)
}
dados_pessoais();
